{
  "name": "ag-ui-worker-example",
  "version": "0.1.0",
  "description": "AG-UI WASM Cloudflare Worker - Pure Rust Implementation Demo",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir ./pkg --release",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "clean": "rm -rf pkg/ node_modules/"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/attackordie/ag-ui.git",
    "directory": "rust-sdk/ag-ui-wasm/examples/worker"
  },
  "keywords": [
    "ag-ui",
    "rust",
    "wasm",
    "cloudflare-workers",
    "server-sent-events",
    "streaming"
  ],
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=16.0.0"
  }
} 